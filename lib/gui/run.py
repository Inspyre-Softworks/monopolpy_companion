class MonopolpyCompanion:

    def run(self):
        import lib.common as common
        from lib.gui.gui import gui
        from lib.gui.windows.application import app_window as mainWin

        app_win = mainWin()

        while True:
            eve
